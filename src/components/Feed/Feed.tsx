import React from 'react';

const Feed: React.FC<{name:string}> = ({ name }) => {
    return (
        <div className='feed-container'>
            <div className="feed">
                FEED
            </div>
        </div>
    );
};

export default Feed;